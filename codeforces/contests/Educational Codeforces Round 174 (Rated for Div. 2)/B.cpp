#include <iostream>

using namespace std;
int main () {
    int t;
    cin >> t;
    while (t--) {
        int n, m;
        cin >> n >> m;
        int cell[n][m];
        for (int i = 0 ; i < n; i++) {
            for (int j = 0 ; j < m; j++) {
                cin >> cell[i][j];
            }
        }
        
    }
}