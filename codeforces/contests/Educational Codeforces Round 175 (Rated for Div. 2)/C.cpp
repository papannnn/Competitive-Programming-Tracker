#include <bits/stdc++.h>

using namespace std;
int main () {
    long long t;
    cin >> t;
    while (t--) {
        int n, k;
        cin >> n >> k;
        string s;
        cin >> s;
        vector<int> penaltyArr;
        while (n--) {
            int penalty;
            cin >> penalty;
            penaltyArr.push_back(penalty);
        }

        
    }
}